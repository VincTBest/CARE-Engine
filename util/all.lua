require("util.math")
require("util.other")
require("util.sei")
require("util.draw")
require("util.text")
require("pl.stringx").import()
json = require("util.json")

CARE_CREDITS = {
    {"CARE Engine", "VincTBest"},
    {"Other", "Penlight"}
}
